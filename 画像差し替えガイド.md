# 画像の簡単差し替え方法

LUMINA STUDIO LPでは、2つの方法で画像を簡単に入れ替えられます。

## 🚀 方法1: 自動スクリプトを使う（おすすめ）

### Pythonスクリプト（推奨）
```bash
python3 画像を入れ替える.py
```

### シェルスクリプト
```bash
./画像を入れ替える.sh
```

**特徴:**
- 📁 画像ファイルをドラッグ&ドロップするだけ
- ✅ 自動的に正しい場所にコピー
- 💡 設定ファイルの更新方法も表示
- 🎯 一括入れ替えにも対応

---

## 📝 方法2: 手動で入れ替える

`images-config.js`ファイルを使って全ての画像を一元管理できます。

## 🎯 簡単3ステップ

### 1. 画像ファイルを準備
実際のLUMINA生成画像を用意します。

### 2. 画像を配置
```bash
# assets/images/ フォルダに画像をコピー
cp /path/to/your/before-image.png "LUMINA STUDIO LP/assets/images/before.png"
cp /path/to/your/after-image.png "LUMINA STUDIO LP/assets/images/after.png"
```

### 3. images-config.js を編集
`images-config.js`ファイルを開いて、画像パスを変更：

```javascript
const IMAGES = {
    heroBackground: 'assets/images/hero-bg.jpg',  // ← ヒーロー背景画像（オプション）
    
    hero: {
        before: 'assets/images/before.png',  // ← ここを変更
        after: 'assets/images/after.png'     // ← ここを変更
    },
    
    samples: [
        'assets/images/sample1.png',  // ← ここを変更
        'assets/images/sample2.png',  // ← ここを変更
        'assets/images/sample3.png'   // ← ここを変更
    ],
    
    useCases: {
        ec: 'assets/images/usecase_ec.png',   // ← ここを変更
        sns: 'assets/images/usecase_sns.png', // ← ここを変更
        ad: 'assets/images/sample1.png'       // ← ここを変更
    }
};
```

ブラウザをリロード（⌘+R）すれば、新しい画像が反映されます！

## 📋 必要な画像一覧

| 用途 | ファイル名 | 推奨サイズ | 設定場所 |
|------|-----------|-----------|---------|
| **Hero背景** | `hero-bg.jpg` | 1920x1080px以上 | `IMAGES.heroBackground` |
| Hero Before | `before.png` | 800x1000px以上 | `IMAGES.hero.before` |
| Hero After | `after.png` | 800x1000px以上 | `IMAGES.hero.after` |
| サンプル1 | `sample1.png` | 600x800px以上 | `IMAGES.samples[0]` |
| サンプル2 | `sample2.png` | 600x800px以上 | `IMAGES.samples[1]` |
| サンプル3 | `sample3.png` | 600x800px以上 | `IMAGES.samples[2]` |
| EC用途 | `usecase_ec.png` | 1200x800px以上 | `IMAGES.useCases.ec` |
| SNS用途 | `usecase_sns.png` | 1200x800px以上 | `IMAGES.useCases.sns` |
| 広告用途 | 任意 | 1200x800px以上 | `IMAGES.useCases.ad` |

## 💡 ヒント

### 異なるファイル名を使う場合
```javascript
// 例：自分のファイル名を使う
hero: {
    before: 'assets/images/my-product-photo.jpg',
    after: 'assets/images/my-ai-generated.jpg'
}
```

### サブフォルダを使う場合
```javascript
// 例：カテゴリ別にフォルダ分け
samples: [
    'assets/images/samples/casual-style.png',
    'assets/images/samples/formal-style.png',
    'assets/images/samples/street-style.png'
]
```

### 外部URLを使う場合
```javascript
// 例：CDNや外部ホスティングの画像を使う
hero: {
    before: 'https://example.com/images/before.png',
    after: 'https://example.com/images/after.png'
}
```

### Hero背景画像を設定/解除する場合
```javascript
// 背景画像を設定
heroBackground: 'assets/images/hero-bg.jpg',

// 背景画像を解除（グラデーション背景に戻す）
heroBackground: null,
```

## ⚠️ 注意点

- ファイルパスは `assets/images/` からの相対パスで指定
- 画像ファイル名に日本語や特殊文字は使わない
- 推奨サイズより小さい画像は画質が粗くなる可能性あり
- 変更後は必ずブラウザをリロード（⌘+R）

## 🔄 元に戻す方法

プレースホルダー画像に戻したい場合：

```javascript
const IMAGES = {
    hero: {
        before: 'assets/images/before.png',
        after: 'assets/images/after.png'
    },
    samples: [
        'assets/images/sample1.png',
        'assets/images/sample2.png',
        'assets/images/sample3.png'
    ],
    useCases: {
        ec: 'assets/images/usecase_ec.png',
        sns: 'assets/images/usecase_sns.png',
        ad: 'assets/images/sample1.png'
    }
};
```
